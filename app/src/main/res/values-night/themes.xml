<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.GitNet" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/dark_background</item>
        <item name="colorPrimaryVariant">@color/white_dark</item>
        <item name="colorOnPrimary">@color/white_dark</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/white_dark</item>
        <item name="colorSecondaryVariant">@color/dark_background</item>
        <item name="colorOnSecondary">@color/dark_tab</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimary</item>
        <!-- Customize your theme here. -->
        <item name="cardBackgroundColor">@color/dark_tab</item>
    </style>
</resources>